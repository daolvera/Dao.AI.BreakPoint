// This file is generated at runtime in production containers.
// For local development, this empty config causes the app to use relative URLs
// which the Angular dev server proxy handles.
(function(window) {
  window.__env = window.__env || {};
  // Leave apiUrl empty for local dev - the proxy.conf.js handles /api routes
  window.__env.apiUrl = '';
})(this);
